import {useState, useEffect} from 'react'

const MyListings = ({addspot}) => {

    // useEffect(() => {
    //     fetch()
    //     .then(r => r.json)
    //     .then(data => )
    // }, [])

    return (
        <div>

        </div>
    )
}

export default MyListings